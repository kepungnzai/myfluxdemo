# apiVersion: helm.toolkit.fluxcd.io/v2
# kind: HelmRelease
# metadata:
#   name: rancher
#   namespace: cattle-system
# spec:
#   dependsOn:
#     - name: cert-manager
#       namespace: cert-manager
#   chart:
#     spec:
#       chart: rancher
#       version: "2.12.3" # Explicitly locking to your requested version
#       sourceRef:
#         kind: HelmRepository
#         name: rancher-stable
#         namespace: flux-system
#   interval: 1h
#   install:
#     createNamespace: true
#   values:
#     hostname: rancher.yourdomain.com
#     replicas: 1
#     bootstrapPassword: "admin-password-here" # Recommended to use a SecretRef instead
#     ingress:
#       tls:
#         source: rancher # Uses cert-manager to generate a self-signed CA